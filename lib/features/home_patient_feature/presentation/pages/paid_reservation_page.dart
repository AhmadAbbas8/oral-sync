import 'package:flutter/material.dart';

class PaidReservationPage extends StatelessWidget {
  const PaidReservationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('PaidReservationPage'),
    );
  }
}
