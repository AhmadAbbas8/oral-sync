import 'package:flutter/material.dart';

class StudentArchivedScreen extends StatelessWidget {
  static const String routeName = '/StudentArchivedScreen';
  const StudentArchivedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
